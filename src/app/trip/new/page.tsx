"use client";

export default function CreateTripPage() {
  return (
    <main className="flex min-h-dvh flex-col justify-center bg-background px-5 pt-[env(safe-area-inset-top)] pb-[env(safe-area-inset-bottom)] text-foreground">
      <section className="mx-auto w-full max-w-md">
        <h1 className="text-3xl font-semibold">Create Trip</h1>
        <p className="mt-3 text-sm text-foreground/65">
          Stub for setting up the first local trip.
        </p>
      </section>
    </main>
  );
}
